apiVersion: apps/v1
kind: Deployment                                          
metadata:
  name: hr-microservice
spec:
  selector:                                              
    matchLabels:
      app: hr-microservice
  replicas: 1                                         
  minReadySeconds: 15
  strategy:
    type: RollingUpdate                                   
    rollingUpdate: 
      maxUnavailable: 0                                   
      maxSurge: 1                                        
  template:                                               
    metadata:
      labels:
        app: hr-microservice                                  
    spec:
      containers:
        - image: thushanijay/humidity-reader-ms:new
          imagePullPolicy: Always                        
          name: hr-microservice
          ports:
            - containerPort: 8004
          volumeMounts:
            - name: readings
              mountPath: /readings
      volumes:
        - name: readings
          persistentVolumeClaim:
            claimName: my-pvc-readings